# =============================================================================
# Edge Inference VLM - Edge Client Package
# =============================================================================
# This package contains the edge-side components responsible for screen capture,
# CLIP vision encoding, and embedding transmission to the server.
# Only abstract float embeddings leave the device — never raw screenshots.
# =============================================================================
